# Optional database URL to use PostgreSQL instead of default SQLite
# Example: postgresql+psycopg2://user:password@host:5432/dbname
DATABASE_URL=

# Flask environment mapping (development|testing|production)
REACT_APP_NODE_ENV=development

# Flask secret key
SECRET_KEY=change-me
